import { StyleSheet } from "react-native";
import colors from '../../../styles/Colors'
export default StyleSheet.create({
    logo: {
        fontSize: 150,
        color: colors.purple,
        margin: 15
    }
})